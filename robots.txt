User-agent: *
Allow: /
# _og.html is only the render source for img/og-cover.png (the link-share card), not a page.
Disallow: /_og.html
# /download/ is gone. It served a v0.3.2 APK (the app is 1.0.2) that was `debuggable` and signed with
# `CN=Android Debug` — the universal debug key shipped in every Android SDK, which anyone on earth has.
# On a PUBLIC site. Anyone who installed it is permanently stranded: a Play upgrade fails on signature
# mismatch, forever. De-indexing it (which is all this line used to do) left it fully downloadable —
# robots is a request to crawlers, not a lock. Deleted 2026-07-17.
Disallow: /download/
Sitemap: https://watchwalks.com/sitemap.xml
